{
  "name": "loopr-contracts",
  "version": "1.0.0",
  "description": "NFT Signing Contracts",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "check:sepolia": "hardhat run scripts/checkBalance.js --network sepolia",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "deploy:example": "hardhat run scripts/deployExample.js --network sepolia",
    "verify:sepolia": "hardhat verify --network sepolia"
  },
  "keywords": [
    "solidity",
    "hardhat",
    "nft"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.8",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.11",
    "hardhat": "^2.22.16"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.2.0",
    "dotenv": "^16.4.7",
    "ethers": "^6.13.4"
  }
}

